<%= tinymce_assets %>
<%= tinymce %>
<%= form_for [:admin, @contacts] do |f| %>
  <div class="field">
    <%= f.label :contacts %><br />
    <%= f.text_area :contacts, :class => 'tinymce', :rows => 30, :cols => 120 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
